# RU Market Analyst — набор скиллов для Claude

Аналитик по рынку РФ (облигации, фонды, акции). Раньше — один скилл-монолит, теперь
**набор инвокабельных скиллов** для точечной работы + **оркестратор** для сквозного
разбора портфеля. На вход — брокерские отчёты (ТБанк PDF, Альфа/Сбер XLS, скрины). На
выходе — сведённый портфель, риск-разбор по корзинам и **таблица рекомендаций (что
убрать → на что заменить) с метриками до/после и кандидатами строго из cbonds**.

> ⚠️ **Дисклеймер.** Это разбор/мысль, не индивидуальная инвест-рекомендация; Claude не
> лицензированный финсоветник. Размер, цена, тайминг и исполнение — за пользователем.
> Скилл только **читает** cbonds/приложения и **не хардкодит** рынок — читает вживую.

---

## Состав набора

| Скилл | Точечная задача | Триггер |
|---|---|---|
| **[ru-market-analyst](ru-market-analyst/SKILL.md)** | сквозной разбор портфеля (оркестратор) | отчёты / «свести портфель / что поменять / стратегия» |
| **[broker-parse](broker-parse/SKILL.md)** | распарсить отчёт → позиции | «распарси этот отчёт» |
| **[cbonds-research](cbonds-research/SKILL.md)** | разобрать бумагу по ISIN (cbonds, проспект) | «что по этой облигации / ISIN» |
| **[market-data](market-data/SKILL.md)** | цена/ставка/курс/кривая (MOEX/ЦБ) | «дай цену/ставку/курс» |
| **[risk-strategy](risk-strategy/SKILL.md)** | корзины + режим + «что если» → ТОП-5 | «корзины/стратегия по этим позициям» |
| **[portfolio-kb](portfolio-kb/SKILL.md)** | база знаний портфеля + дифф (опц.) | «сохрани/обнови базу, что изменилось» |
| **[ru-market-shared](ru-market-shared/SKILL.md)** | общий справочный пак (инструменты/источники/налоги) | внутренний, не триггерится напрямую |

Оркестратор `ru-market-analyst` проходит фазы **inline-routing’ом** (читает и следует
SKILL.md нужного суб-скилла): broker-parse → cbonds-research → market-data →
risk-strategy (+ portfolio-kb по запросу). Каждый суб-скилл можно вызвать и отдельно.

## Главная фишка
- **🎯 Таблица рекомендаций:** что убрать/сократить → на что заменить (**кандидат из
  cbonds, не из головы**), с **метриками до/после** и строкой сверки стоимости.
- **🔁 «Что если» как поиск → ТОП-5 вариантов.** Сначала — **цель** (перформанс /
  надёжность / ультра / баланс / доход), затем поиск портфелей-вариантов → топ-5 (выбрать
  или понять направление). Глубокий поиск токеноёмкий — скилл предупреждает и спрашивает
  глубину.
- **📡 No-hardcode:** ставка/курс/цены/режим — вживую (ЦБ/MOEX/cbonds), не из памяти.
- Корректность: амортизация секьюритизаций, $-номинал бивалютных, yield-to-worst, сверка
  с марком брокера; налоги (ЛДВ/ИИС; **фонд vs прямые бумаги** — нетто по типу счёта:
  экономия TER − налог на купон у прямых, который БПИФ не платит внутри фонда).
  Структурные ноты — применимые условия из приложения, если cbonds их не даёт.

## Требования
| Что | Зачем | Обязательно |
|-----|-------|-------------|
| Подписка **cbonds** | первоисточник по инструментам | да, для ресёрча |
| **Claude in Chrome** | чтение cbonds через залогиненный браузер | да, для cbonds |
| Веб-доступ (`web_fetch`) | MOEX ISS + ЦБ (цены/кривая/ставка/курс) | да |
| **Python 3 (venv)** | парсинг PDF/старых .xls (`pypdf`/`xlrd`) | фолбэк, если нет готовых тулзов |

## Установка
```bash
git clone https://github.com/TimurTsedik/ru-market-analyst.git
```
**Claude Code — ставь ВСЕ папки соседями** в `~/.claude/skills/` (копией или симлинком),
включая `ru-market-shared` — иначе sibling-ссылки `../ru-market-shared/...` не резолвятся:
```
~/.claude/skills/ru-market-analyst   → <repo>/ru-market-analyst
~/.claude/skills/broker-parse        → <repo>/broker-parse
~/.claude/skills/cbonds-research     → <repo>/cbonds-research
~/.claude/skills/market-data         → <repo>/market-data
~/.claude/skills/risk-strategy       → <repo>/risk-strategy
~/.claude/skills/portfolio-kb        → <repo>/portfolio-kb
~/.claude/skills/ru-market-shared    → <repo>/ru-market-shared
```
Подхватятся при старте сессии. **Claude.ai:** нужен **бандл со всеми папками** (один
пакет, сохраняющий соседство папок); отдельные zip суб-скиллов и «только оркестратор»
не поддерживаются — sibling-ссылки `../...` не резолвятся.

`tests/` и приватные данные (база знаний портфеля) в репозиторий не входят.

## Принципы
- cbonds первичен по инструментам; рыночная оценка брокера — по стоимости портфеля.
- Читай рынок вживую, не из памяти. Проверяй имена, не вешай ярлыки.
- После каждого рассуждения — основание; в конце — дисклеймер.
- Кандидаты на замену — только из cbonds (верифицированы), не из головы.

## Недавние изменения
- **2026-06: налоговый фактор «фонд vs прямые бумаги» (нетто-расчёт).** При сравнении
  «держать облигационный БПИФ vs переложиться в прямые ОФЗ» нельзя считать выигрышем
  одну экономию TER: БПИФ структурно не платит налог с купона внутри фонда, а прямой
  держатель на **брокерском** счёте платит НДФЛ с купона **ежегодно** (~1,3–1,8 п.п.),
  что во многом съедает TER (~1,5%), плюс разовый НДФЛ при продаже паёв без ЛДВ. Вывод
  **зависит от типа счёта**: на брокерском замена может быть нейтральной/отрицательной,
  на ИИС — выигрыш. Добавлены: обязательный гвард-нетто в `risk-strategy/.../strategy-engine.md`
  (§3 + скоринг §5) и факт в `ru-market-shared/.../taxes-accounts.md`. Прогнано через
  codex-ревью (методология: критическая оценка, в т.ч. налоговые нюансы ИИС-А/Б/ИИС-3).

## Лицензия / ответственность
Используйте на свой риск. Скилл даёт аналитический разбор, а не инвест-рекомендацию;
ответственность за решения и сделки — на пользователе.
